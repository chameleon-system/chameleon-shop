{#
    @var int level
    @var ChameleonSystem\EcommerceStatsBundle\Service\EcommerceStatsGroup[] blocks
    @var string[] columnNames
    @var bool showDiffColumn
    @var int maxGroupCount
#}

<div class="TCMSGroupedStatistics">
    <div class="html-barchart">
        {% for block in blocks %}
            <div class="barchart-groupname">{{ block.groupTitle }}</div> <br />
                <div class="barchart-body">
                    {{ include( '@ChameleonSystemEcommerceStats/snippets-cms/ecommerceStats/module/barchart-body.html.twig' , { group: block, chartId: loop.index }) }}
                </div>
            <div class="cleardiv"></div> <br />
        {% endfor %}
</div>
